import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vh.onsitesync',
  appName: 'OnsiteSync',
  webDir: 'dist/onsite-sync/browser',
};

export default config;
